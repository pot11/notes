# windows上初始化

```shell
git config --global user.name "pot11"
git config --global user.email "persistpot@gmail.com"
git config --global http.proxy http://127.0.0.1:7890 
git config --global https.proxy http://127.0.0.1:7890
```
