
```json
1：用户输入trade_id，然后调用订单分析服务：
http://10.161.12.116:63663/butterfly/fzyw?easyoper=1&FUN_CODE=D026&TRADE_ID=trade_id ，
获取具体的报错信息输出，如报错信息为：
"调用delvs/cdtuserworker/exec异常
{
    "MSG": "服务调用成功！",
    "STATUS": "0000",
    "TXID": "fdc814a56831^1770276379281^1287830",
    "RSP": {
        "SUB_CODE": "C699",
        "RSP_DESC": "调用服务users_usernowoperting报错：调用服务users_userchanging报错：操作数据库失败：Duplicate entry '3024042484080944-91088967-2026-04-01 00:00:00' for key 'PRIMARY' More"
    }
}
并将输出的报错信息中,截取报错字段信息为entry字符串后''引号之间的内容，即：'3024042484080944-91088967-2026-04-01 00:00:00'，并将引号之间的内容分为3个字段：'user_ida-key1-start_datea'；
user_ida、key1、start_datea

2：用1输入的trade_id，调 订单查询服务：http://10.1.0.126:4936/trades/delvlogs/tradeinfo获取订单的组织信息；
入参为
{
	"REQ": {
		"QRY_TYPE": "6",
		"QRY_VALUE": "",
		"SWITCH": "1",
		"STATUS": "3",
		"TAG_SET": "111111111111111111111111111111111111111111111111111111111111111111111"
	}
}
QRY_VALUE为第1步中输入的trade_id，输出的结果封装到json1中；
3：用第1步的获取的user_ida作为入参，调用：用户资料查询服务：http://10.1.0.126:4936/users/userfullinfo获取用户的资料信息；
入参为
{
	"REQ": {
		"OPER_TYPE": "4",
		"USER_INFO_TAG_SET": "2222222222222222222222222222222222222222222222",
		"NEED_RETURN_ROUTE": "1",
		"USER_INFO": {
			"USER_ID": ""
		}
	}
}
USER_ID为第1步的获取的user_ida；输出的结果封装到json2中；

4：通过第一步截取的报错字段组合user_ida、key1、start_datea，
取json1文件中RSP.DATA节点下的任意一个节点中的内容，如果该节点下的user_ida=USER_ID，且start_datea=START_DATE,且key1与该节点下的PRODUCT_ID或SERVICE_ID或DISCNT_CODE相等，
且取得的相等的节点个数大于1，
则调用adb查询服务：http://10.161.12.116:45566/cqy/query，
如果key1与该节点下的PRODUCT_ID相等，
则入参为：为SELECT * FROM D_ORDER.TF_B_TRADE_PRODUCT WHERE USER_ID=user_ida and PRODUCT_ID=key1 and start_datea=START_DATE，若能够查询到该数据，且数量条数大于1;
根据该入参，给我输出DELETE语句：DELETE * FROM D_ORDER.TF_B_TRADE_PRODUCT WHERE USER_ID=user_ida and PRODUCT_ID=key1 and start_datea=START_DATE limit 1;
如果key1与该节点下的DISCNT_CODE相等，
则入参为：为SELECT * FROM D_ORDER.TF_B_TRADE_DISCNT WHERE USER_ID=user_ida and DISCNT_CODE=key1 and start_datea=START_DATE，若能够查询到该数据，且数量条数大于1;
根据该入参，给我输出DELETE语句：DELETE * FROM D_ORDER.TF_B_TRADE_DISCNT WHERE USER_ID=user_ida and DISCNT_CODE=key1 and start_datea=START_DATE limit 1;
如果key1与该节点下的SERVICE_ID相等，
则入参为：为SELECT * FROM D_ORDER.TF_B_TRADE_SERVICE WHERE USER_ID=user_ida and SERVICE_ID=key1 and start_datea=START_DATE，若能够查询到该数据，且数量条数大于1;
根据该入参，给我输出DELETE语句：DELETE * FROM D_ORDER.TF_B_TRADE_SERVICE WHERE USER_ID=user_ida and SERVICE_ID=key1 and start_datea=START_DATE limit 1;

5：通过第一步截取的报错字段组合user_ida-key1-start_datea，
取json2文件中RSP.DATA节点下的任意一个节点中的内容，如果该节点下的user_ida=USER_ID，且start_datea=START_DATE,且key1与该节点下的PRODUCT_ID或SERVICE_ID或DISCNT_CODE相等，
则调用adb查询服务：http://10.161.12.116:45566/cqy/query，
如果key1与该节点下的PRODUCT_ID相等，
则入参为：为SELECT * FROM D_USERS.TF_F_USER_PRODUCT WHERE USER_ID=user_ida and PRODUCT_ID=key1 and start_datea=START_DATE，若能够查询到该条数据;
根据该入参，给我输出DELETE语句：DELETE * FROM D_USERS.TF_F_USER_PRODUCT WHERE USER_ID=user_ida and PRODUCT_ID=key1 and start_datea=START_DATE;
如果key1与该节点下的DISCNT_CODE相等，
则入参为：为SELECT * FROM D_USERS.TF_F_USER_DISCNT WHERE USER_ID=user_ida and DISCNT_CODE=key1 and start_datea=START_DATE，若能够查询到该条数据;
根据该入参，给我输出DELETE语句：DELETE * FROM D_USERS.TF_F_USER_DISCNT WHERE USER_ID=user_ida and DISCNT_CODE=key1 and start_datea=START_DATE;
如果key1与该节点下的SERVICE_ID相等，
则入参为：为SELECT * FROM D_USERS.TF_F_USER_SERVICE WHERE USER_ID=user_ida and SERVICE_ID=key1 and start_datea=START_DATE，若能够查询到该条数据;
根据该入参，给我输出DELETE语句：DELETE * FROM D_USERS.TF_F_USER_SERVICE WHERE USER_ID=user_ida and SERVICE_ID=key1 and start_datea=START_DATE;
```json