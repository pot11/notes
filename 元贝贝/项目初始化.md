# 项目初始化


## 释放mac上端口

```shell
port=7800; pids=$(lsof -ti :$port); if [ -n "$pids" ]; then kill -9 $pids; echo "端口 $port 已释放"; else echo "端口 $port 未被占用"; fi
port=9990; pids=$(lsof -ti :$port); if [ -n "$pids" ]; then kill -9 $pids; echo "端口 $port 已释放"; else echo "端口 $port 未被占用"; fi
port=9500; pids=$(lsof -ti :$port); if [ -n "$pids" ]; then kill -9 $pids; echo "端口 $port 已释放"; else echo "端口 $port 未被占用"; fi
port=9114; pids=$(lsof -ti :$port); if [ -n "$pids" ]; then kill -9 $pids; echo "端口 $port 已释放"; else echo "端口 $port 未被占用"; fi
port=9112; pids=$(lsof -ti :$port); if [ -n "$pids" ]; then kill -9 $pids; echo "端口 $port 已释放"; else echo "端口 $port 未被占用"; fi
port=9991; pids=$(lsof -ti :$port); if [ -n "$pids" ]; then kill -9 $pids; echo "端口 $port 已释放"; else echo "端口 $port 未被占用"; fi
port=9998; pids=$(lsof -ti :$port); if [ -n "$pids" ]; then kill -9 $pids; echo "端口 $port 已释放"; else echo "端口 $port 未被占用"; fi
port=9113; pids=$(lsof -ti :$port); if [ -n "$pids" ]; then kill -9 $pids; echo "端口 $port 已释放"; else echo "端口 $port 未被占用"; fi
```

## 代码地址

```shell
https://codeup.aliyun.com/67e242bc8571f8159f9a9d0e/ybb-backend/ybb-message.git

https://codeup.aliyun.com/67e242bc8571f8159f9a9d0e/ybb-backend/ybb-user.git

https://codeup.aliyun.com/67e242bc8571f8159f9a9d0e/ybb-backend/ybb-gateway.git

https://codeup.aliyun.com/67e242bc8571f8159f9a9d0e/ybb-backend/ybb-common.git

https://codeup.aliyun.com/67e242bc8571f8159f9a9d0e/ybb-backend/ybb-specpkg.git

https://codeup.aliyun.com/67e242bc8571f8159f9a9d0e/ybb-backend/ybb-inquiry.git

https://codeup.aliyun.com/67e242bc8571f8159f9a9d0e/ybb-backend/ybb-platform.git

https://codeup.aliyun.com/67e242bc8571f8159f9a9d0e/ybb-backend/ybb-physician.git

https://codeup.aliyun.com/67e242bc8571f8159f9a9d0e/ybb-backend/yl-common.git

https://codeup.aliyun.com/67e242bc8571f8159f9a9d0e/ybb-backend/ybb-uc.git
```